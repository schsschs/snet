/**
@file  utility.h
@brief SNet utility header
*/
#ifndef __SNET_UTILITY_H__
#define __SNET_UTILITY_H__

#define SNET_MAX(x, y) ((x) > (y) ? (x) : (y))
#define SNET_MIN(x, y) ((x) < (y) ? (x) : (y))

#endif /* __SNET_UTILITY_H__ */

